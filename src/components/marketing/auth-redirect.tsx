"use client";

/**
 * Placeholder — previously redirected authenticated users away from marketing
 * pages. Disabled so all pages (landing, privacy, terms, support) are viewable
 * regardless of auth state.
 */
export function AuthRedirect() {
	return null;
}
